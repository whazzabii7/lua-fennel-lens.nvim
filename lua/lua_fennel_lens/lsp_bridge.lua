-- lua/fennel_lens/lsp_bridge.lua
local M = {}

-- FUTURE: extract Fennel code and attach LSP
function M.setup()
  -- placeholder for LSP integration
  -- e.g., scan Lua buffers for fennel.eval calls and open virtual .fnl buffers
end

return M
